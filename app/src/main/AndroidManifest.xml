<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.valdeslab.learningapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NavigationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DataMovement.BasicExampleActivity" />
        <activity android:name=".DataMovement.FragmentExampleActivity" />
        <activity android:name=".DataMovement.DataCollectionActivity" />
        <activity android:name=".RecyclerView.RecyclerViewActivity"></activity>
    </application>

</manifest>